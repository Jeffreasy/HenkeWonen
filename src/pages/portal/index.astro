---
import PortalLayout from "../../components/layout/PortalLayout.astro";
import DashboardShell from "../../components/dashboard/DashboardShell";
import { PageHeader } from "../../components/ui/PageHeader";

const session = Astro.locals.session;

if (!session) {
  return Astro.redirect("/login");
}
---

<PortalLayout title="Overzicht" session={session}>
  <PageHeader
    eyebrow="Start"
    title="Henke Wonen werkoverzicht"
    description="Begin bij wat aandacht vraagt en werk daarna verder vanuit het juiste dossier."
  />

  <DashboardShell client:load session={session} />
</PortalLayout>
